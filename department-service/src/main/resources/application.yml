server:
  port: 9001
 
spring:
  application:
    name: DEPARTMENT-SERVICE


eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultzone: http://localhost:8761/eureka/
